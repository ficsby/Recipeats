import * as firebase from "firebase";
import Macro from "../data/Macro";

export const calculateGoals = (height, weight, activityLevel) => {
    //find a formula to calculate this (similar to MyFitnessPal)
}

